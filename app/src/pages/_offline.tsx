import React from "react";
import Layout from "../components/Layout/Layout";

type Props = {};

const _offline = (props: Props) => {
  return <Layout>Estamos Offline</Layout>;
};

export default _offline;
