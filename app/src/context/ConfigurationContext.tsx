// import { createContext, useContext } from "react";

// interface ConfContext { 
//   eso: string;
// }

// export const confContext = createContext<ConfContext|undefined>(undefined)

// export const useConfiguration = () => { 
//   const context = useContext(confContext)
//   if (!context) throw new Error("There is no configuration");
//   return context;
// }

// export function ConfigurationProvider () { 

// }